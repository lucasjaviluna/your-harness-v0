# Pi Harness consumer fixture

Repositorio consumidor mínimo para validar que `pi-harness` puede cargarse desde fuera de su raíz.

La prueba se ejecuta usando el package local como fuente de extensión, sin instalarlo globalmente.
